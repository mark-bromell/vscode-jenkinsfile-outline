import { Stage } from "./structure";

export function parseJenkinsFile(lines: string[]) : Stage[] {
    for (const line in lines) {
        
    }
    return [];
}

export function isStageOpen(line: string) : boolean {
    return false;
}

export function isStageClose(line: string) : boolean {
    return false;
}

export function isGenericOpen(line: string) : boolean {
    return false;
}

export function isGenericClose(line: string) : boolean {
    return false;
}
